import 'package:flutter/material.dart';

class AppLocalizations {
  static List<Locale> supportedLocales = [
    const Locale('tr'),
    const Locale('en')
  ];

  static Locale trLocale = const Locale('tr');

  static String path = 'assets/translations';

}
